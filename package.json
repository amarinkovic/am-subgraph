{
  "name": "am-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ am-subgraph",
    "create-local": "graph create --node http://localhost:8020/ am-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ am-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 am-subgraph",
    "watch-local": "graph deploy am-subgraph --debug --node http://127.0.0.1:8020/ --ipfs http://localhost:5001"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
