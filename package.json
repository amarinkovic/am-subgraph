{
  "name": "am-nayms-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ am-nayms-test",
    "create-local": "graph create --node http://localhost:8020/ am-nayms-test",
    "remove-local": "graph remove --node http://localhost:8020/ am-nayms-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 am-nayms-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
